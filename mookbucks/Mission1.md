``` js
// 오늘 얻은 인사이트

// 1. 이벤트 위임을 어떻게 하는지에 대해 제대로 이해했음

// 2. 요구사항을 전략적으로 접근하는 방법, 또한, 단계별로 나누는 것이 중요하다는 것을 이해함

// 3. Dom 요소를 가져올 때는 변수처럼 사용할 수 있다. 제이쿼리 문법이랑 비슷한듯 $()

// 4. 새롭게 알게된 메소드 insertAdjacentHTML, closest, confirm

// 5. 공통적인 부분은 함수로 만들어서 사용하자

// 6. 기능구현을 마치면 리팩토링을 반드시 하는 습관을 들이자

// 7. 리팩토링 시 목적에 맞게끔 같이 모아두는 것이 훨씬 깔끔하다. 공통함수들을 같이 모은 것처럼!

  

// step1 요구사항 구현을 위한 전략

// TODO 메뉴 추가

// - [x] 메뉴의 이름을 입력 받고 엔터키 입력으로 추가한다.

// - [x] 메뉴의 이름을 입력 받고 확인 버튼을 클릭하면 메뉴를 추가한다.

// - [x] 추가되는 메뉴의 아래 마크업은 `<ul id="espresso-menu-list" class="mt-3 pl-0"></ul>` 안에 삽입해야 한다.

// - [x] 총 메뉴 갯수를 count하여 상단에 보여준다.

// - [x] 메뉴가 추가되고 나면, input은 빈 값으로 초기화한다.

// - [x] 사용자 입력값이 빈 값이라면 추가되지 않는다.

  

//TODO 메뉴 수정

// - [x] 메뉴의 수정 버튼 클릭 시 이벤트를 받고, 수정하는 모달창(prompt이 뜬다.

// - [x] 모달창에서 신규메뉴명을 입력 받고, 확인 버튼을 누르면 메뉴가 수정된다.

  

//TODO 메뉴 삭제

// - [x] 메뉴 삭제 버튼 이벤트를 받고, 메뉴 삭제 컨펌(confirm) 모달창이 뜬다.

// - [x] 확인 버튼을 클릭하면 메뉴가 삭제된다.

// - [x] 총 메뉴 갯수를 count하여 상단에 보여준다.
```